# todo: fix this so it works within this monorepo
# currently it's not working because .github needs to be at repo root, and this is within a subfolder
# would need to update the `on` to include the subfolder path

name: CI - Theo Drive Tutorial

on:
  push:
    paths:
      - ".github/workflows/**"
      - "/home/pedro/dev/learn/javascript/nextjs/01-theo-drive-tutorial"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      # wouldn't be needed if we found an image with pnpm installed
      - name: Install Dependencies
        run: npm install -g pnpm && pnpm install

      # stops the build from failing if env vars are missing
      - name: Copy .env.example files
        shell: bash
        run: find . -type f -name ".env.example" -exec sh -c 'cp "$1" "${1%.*}"' _ {} \;

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint
